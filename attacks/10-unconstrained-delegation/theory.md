# Unconstrained Delegation

# Teoria

O Active Directory permite que um servidor se passe por um usuário para realizar tarefas em nome dele (Delegação).

No Unconstrained Delegation, se um administrador logar em um servidor comprometido, esse servidor captura o TGT do administrador e o atacante pode usá-lo para controlar todo o domínio.
